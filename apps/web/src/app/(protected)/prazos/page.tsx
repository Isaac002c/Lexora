import Link from "next/link";
import { ApiActionButton } from "@/components/api-action-button";
import { CreatePanel } from "@/components/create-panel";
import { DataTable } from "@/components/data-table";
import { DeadlineCalendar } from "@/components/deadline-calendar";
import { PageHeader } from "@/components/page-header";
import { SearchForm } from "@/components/search-form";
import { StatusBadge } from "@/components/status-badge";
import { ModuleNav } from "@/features/shared/components/module-nav";
import { formatDate } from "@/lib/format";
import { fetchData, type Lookups } from "@/lib/page-data";

interface DeadlineList { items: Array<{ id: string; branchId: string; caseId: string; clientId: string; legalAreaId: string; responsibleUserId: string; title: string; type: string; dueAt: string; priority: string; status: string; color: string; notes?: string; branch: { name: string }; client: { name: string }; case: { processNumber?: string; caseType: string }; responsibleUser: { name: string } }>; total: number }
type Query = { search?: string; view?: "overdue" | "today" | "next5" | "next7" | "distant" | "completed"; branchId?: string; legalAreaId?: string; responsibleId?: string; status?: string; type?: string };
const types = ["PETICAO_INICIAL", "AUDIENCIA", "RECURSO", "MANIFESTACAO", "ADMINISTRATIVO", "OUTRO"];
export default async function DeadlinesPage({ searchParams }: { searchParams: Promise<Query> }) {
  const query = await searchParams; const params = new URLSearchParams({ pageSize: "100", ...Object.fromEntries(Object.entries(query).filter((entry): entry is [string, string] => Boolean(entry[1]))) });
  const [data, lookups] = await Promise.all([fetchData<DeadlineList>(`/v1/deadlines?${params}`), fetchData<Lookups>("/v1/lookups")]);
  const deadlineFields = [{ name: "title", label: "Título", required: true }, { name: "type", label: "Tipo", type: "select" as const, required: true, options: types.map((value) => ({ value, label: value.replaceAll("_", " ") })) }, { name: "caseId", label: "Processo", type: "select" as const, required: true, options: lookups.cases.map((item) => ({ value: item.id, label: item.name })) }, { name: "clientId", label: "Cliente", type: "select" as const, required: true, options: lookups.clients.map((item) => ({ value: item.id, label: item.name })) }, { name: "branchId", label: "Filial", type: "select" as const, required: true, options: lookups.branches.map((item) => ({ value: item.id, label: item.name })) }, { name: "legalAreaId", label: "Área", type: "select" as const, required: true, options: lookups.legalAreas.map((item) => ({ value: item.id, label: item.name })) }, { name: "responsibleUserId", label: "Responsável", type: "select" as const, required: true, options: lookups.users.map((item) => ({ value: item.id, label: item.name })) }, { name: "dueAt", label: "Vencimento", type: "datetime-local" as const, required: true }, { name: "priority", label: "Prioridade", type: "select" as const, defaultValue: "NORMAL", options: ["LOW", "NORMAL", "HIGH", "URGENT"].map((value) => ({ value, label: value })) }, { name: "notes", label: "Observações", type: "textarea" as const }];
  return <><PageHeader eyebrow={`${data.total} prazos`} title="Prazos" description="Controle operacional de vencimentos, audiências e compromissos." action={<CreatePanel title="Novo prazo" endpoint="/api/v1/deadlines" buttonLabel="Novo prazo" fields={deadlineFields} />} /><ModuleNav items={[{ label: "Todos", href: "/prazos" }, { label: "Vencidos", href: "/prazos?view=overdue" }, { label: "Vencem hoje", href: "/prazos?view=today" }, { label: "Próximos 5 dias", href: "/prazos?view=next5" }, { label: "Próximos 7 dias", href: "/prazos?view=next7" }, { label: "Mais distantes", href: "/prazos?view=distant" }, { label: "Concluídos", href: "/prazos?view=completed" }]} /><SearchForm defaultValue={query.search} placeholder="Título do prazo"><select name="branchId" defaultValue={query.branchId} className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todas as filiais</option>{lookups.branches.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select name="legalAreaId" defaultValue={query.legalAreaId} className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todas as áreas</option>{lookups.legalAreas.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select name="responsibleId" defaultValue={query.responsibleId} className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todos os responsáveis</option>{lookups.users.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select name="type" defaultValue={query.type} className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todos os tipos</option>{types.map((value) => <option key={value} value={value}>{value.replaceAll("_", " ")}</option>)}</select></SearchForm><DeadlineCalendar items={data.items} mode="list" /><DataTable columns={["Prazo", "Cliente / processo", "Responsável", "Vencimento", "Prioridade", "Situação", "Ações"]} rows={data.items.map((item) => [<span key={item.id} className="font-medium">{item.title}</span>, <span key="case"><span className="block">{item.client.name}</span><Link href={`/processos/${item.caseId}`} className="text-xs text-cyan-600">{item.case.processNumber ?? item.case.caseType}</Link></span>, item.responsibleUser.name, formatDate(item.dueAt, true), item.priority, <span key="state" className="flex flex-wrap gap-1"><StatusBadge value={item.status} /><StatusBadge value={item.color} /></span>, <div key="actions" className="flex flex-wrap gap-2"><CreatePanel title={`Editar ${item.title}`} endpoint={`/api/v1/deadlines/${item.id}`} method="PATCH" buttonLabel="Editar" fields={deadlineFields.map((field) => ({ ...field, defaultValue: ({ title: item.title, type: item.type, caseId: item.caseId, clientId: item.clientId, branchId: item.branchId, legalAreaId: item.legalAreaId, responsibleUserId: item.responsibleUserId, dueAt: item.dueAt.slice(0, 16), priority: item.priority, notes: item.notes } as Record<string, string | undefined>)[field.name] ?? field.defaultValue }))} />{item.status === "COMPLETED" ? <ApiActionButton method="PATCH" endpoint={`/api/v1/deadlines/${item.id}/status`} body={{ status: "PENDING" }} label="Reabrir" /> : <ApiActionButton method="PATCH" endpoint={`/api/v1/deadlines/${item.id}/status`} body={{ status: "COMPLETED" }} label="Concluir" />}</div>])} /></>;
}
