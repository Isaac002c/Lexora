import { ChecklistNav } from "@/features/checklists/components/checklist-nav";
export default function ChecklistsLayout({ children }: { children: React.ReactNode }) { return <><ChecklistNav />{children}</>; }
